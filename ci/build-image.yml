---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
- name: backline-src

run:
  path: sh
  args:
    - -exc
    - pwd; ls -la; cd target; ls -la;

outputs:
- name: backline-src
