---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bitnami/kubectl

inputs:
- name: backline-src

run:
  path: backline-src/ci/deploy-to-k8s.sh
#  args: ["((k8s-api))", "((k8s-user))", "((k8s-client-cert))", "((k8s-client-key))", "((k8s-namespace))"]

